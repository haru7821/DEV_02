# 인공신장실(혈액투석실) 도면 에디터

병원 인공신장실의 평면 배치를 그리고, 의료 규격을 자동 검증하며, A4 가로모드 PDF로 출력하는 싱글 페이지 애플리케이션(SPA)입니다.

## 실행 방법

빌드 과정이 없습니다. 저장소를 받은 뒤 정적 서버로 열면 됩니다. (라이브러리는 `vendor/`에 번들되어 있어 인터넷이 없는 병원 내부망에서도 동작합니다.)

```bash
# 예시: 파이썬 내장 서버
python3 -m http.server 8080
# 브라우저에서 http://localhost:8080 접속
```

## 주요 기능

| 기능 | 설명 |
|---|---|
| 새 도면 | 병실 가로/세로 크기(mm)를 입력해 외벽·그리드 생성 (기본 25,000×15,000mm) |
| 도면 사진 불러오기 | 기존 도면 사진을 배경으로 깔고 실제 가로 길이(mm)로 축척 보정 |
| 그리드 스냅 | 100mm / 500mm 단위 자석 정렬, 회전은 15° 단위 스냅 |
| 그룹화 | 침대+투석기+모니터를 하나의 유닛으로 묶어 동시 이동 |
| 정수 배관 그리기 | 클릭으로 경로를 찍고 더블클릭으로 완료 |
| 검증 | 병상 간 이격 거리(기준 1m)·정수 배관 동선·필수 시설을 검사, 위반 객체는 빨간 테두리로 깜빡임 |
| JSON 저장/열기 | 도면 데이터 내보내기·불러오기 (사용자 정의 시설 포함) |
| PDF 출력 | 표제란 포함 A4 가로모드 PDF 다운로드 |
| 벽체 두께 | 50~400mm 설정, 도면에 `벽체 THK ○○mm` 주석 자동 표기 |
| 문 / 개구부 | 여닫이(외짝/양짝)·자동문·미닫이문 — 건축 기호(개폐 호 등)로 렌더링, 속성 패널에서 폭 조절 |
| 병상 번호 | 병상 유닛에 HD1, HD2… 자동 부여, 속성 패널에서 수정, 행→열 순 재정렬 버튼 |
| 급수/배수 배관 | Inlet(파랑 실선)·Drain(갈색 점선) 2계통, 시작점에 IN/DR 라벨 |
| CORE | PS/EPS 샤프트를 대각선 X 기호로 배치 |
| 정수실 설비 | 실제 정수실 시공 도면 기반 14종 — RO 시스템(CWP106H/CWP66), Multimedia·Softner·Carbon 여과탱크(밴드 원형 기호), 5μ 필터, 이송펌프(Auto/Manual), Heating System, Control Box, Main Panel(380V), 원수 인입·자연/강제배수, 220V 콘센트 |
| 사용자 시설 | 이름·크기(mm)·색상을 지정해 임의 시설 등록 (JSON에 함께 저장) |
| 도면 모드 | 흑백 건축 도면(청사진) 스타일 토글 — 배관 색상은 유지 |
| 실행 취소/다시 실행 | Ctrl+Z / Ctrl+Y, 최근 50단계 스냅샷 |
| 복사/붙여넣기/복제 | Ctrl+C/V/D — 크기 조정(스케일)·회전·반전 상태 그대로 복제 |
| 등간격 배치 | 3개 이상 선택 시 가로·세로 간격을 동일하게 배분 |
| 순서·반전·잠금 | 맨 앞/뒤로, 좌우/상하 반전, 객체 잠금(이동·크기·회전 방지, 다중 선택 지원) |
| 잠금 보존 재배치 | 🔒 잠긴 객체는 Auto Modeling 재실행 시 위치가 고정되고 다른 영역만 재배치. 잠긴 병상 주변은 1m 이격 확보, 실·기둥은 회피 배치 |
| 기둥 | 구조 기둥(기본 500×500mm, 속성 패널에서 크기 조절) — 잠그면 자동 배치가 기둥을 피해 감 |
| 줌 컨트롤 | 캔버스 우하단 −/+/맞춤 버튼과 배율(%) 표시 |
| 엣지 자석 | 객체 이동 시 다른 객체의 엣지 120mm 이내로 접근하면 자석처럼 흡착 |
| 크기 스텝 | 드래그 크기 조절이 10mm 단위로 단계 스냅 |
| 병상 모듈 | 침대+투석기 존 = 폭 1800mm × 길이 2200mm(둘 다 설정 가능) 모듈. 모듈끼리 붙여 배치(피치=모듈 폭)하며, 모듈 밀착 병상 쌍은 이격 검증 예외 |
| 발쪽-벽 이격 | 침대 발쪽과 벽/부속실 사이 800mm 미만이면 위반 검출 |
| PNG/SVG 내보내기 | 고해상도 PNG 이미지, 벡터 SVG 파일 다운로드 |
| Auto Modeling | 시설 선택 + 목표 병상 대수 + 통로 폭(기본 1500mm) 입력 → 규격을 지키며 병상을 자동 설계. 누를 때마다 다른 구성(서비스 존 방향·시설 순서·병상 간격) 생성 |
| 배관 콘솔 | 투석 장비 머리맡을 따라 급수/배수 배관이 지나가는 설비 덕트. 상단 네비에서 두께(100~800mm, 기본 600 — reference-plans 실측 640mm) 지정, 자동 배치 시 병상 열마다 자동 생성 |
| 동선 설계 | 주 출입구를 간호 스테이션 옆에 배치하고, 출입구→스테이션→병상으로 이어지는 주 동선 통로(음영+화살표)를 확보 — 병상이 통로를 침범하지 않음 |
| 실 규격 통일 | 자동 배치 시 측면 밴드는 가로 폭, 하단 밴드는 세로 깊이를 통일해 정돈된 평면 구성 |
| 안쪽 열림 문 | 자동 배치되는 여닫이문은 모두 벽면 기준 실 내부로 열림 |
| Sub 툴바 | 좌측 카테고리를 클릭하면 하위 항목이 펼쳐지는 아코디언 구조. 편집·정렬·도구 명령은 상단 커맨드 바에 배치 |

## 파일 구조

```
index.html          전체 UI 레이아웃 (상단 네비 · 좌측 툴바 · 중앙 캔버스 · 우측 속성 패널)
style.css           UI 디자인 및 캔버스 그리드 스타일
vendor/             Fabric.js 5.3.0, jsPDF 2.5.1 (로컬 번들)
js/
 ├ app.js           메인 실행 파일 — 초기화, 이벤트 리스너, PDF/JSON 입출력
 ├ assets.js        장비 데이터 정의 (규격 cm, 색상, requiresWater 등) + 의료 규격 상수
 ├ canvas.js        Fabric.js 캔버스 제어 — 배치·이동·회전·스냅·그룹화·자동 배치
 └ validation.js    의료 규격 검증 — 병상 이격 거리, 배관 동선, 필수 시설
```

## 좌표계와 축척

캔버스 모델 좌표 **1px = 실제 1cm**로 고정하고, 화면 표시는 Fabric.js의 zoom으로만 조절합니다.
따라서 객체의 `left / top / width / height` 값이 곧 실제 치수(cm)이며, 검증 로직이 별도 환산 없이 동작합니다.
**UI의 모든 수치 입력·표시는 mm 단위**이며, `app.js`의 `toMM()/toCM()` 헬퍼가 화면 경계에서만 변환합니다.

## 병상 간격 검증 공식

두 병상의 (회전 반영) 바운딩 박스 A, B에 대해 **테두리 간 최단 거리**를 계산합니다.

```
dx = max(0, A.left − B.right, B.left − A.right)   // 가로축 이격
dy = max(0, A.top − B.bottom, B.top − A.bottom)   // 세로축 이격
gap = Math.hypot(dx, dy) = √(dx² + dy²)           // 피타고라스 정리
```

- 겹치면 `dx = dy = 0` → `gap = 0`
- 좌우로만 떨어져 있으면 `gap = dx` (대각선 아님)
- 대각선 방향이면 모서리 간 거리 `√(dx² + dy²)`

`gap < 100cm`(기준 1,000mm)이면 위반으로 판정하며, 화면에는 mm로 표시됩니다. 기준값은 `js/assets.js`의 `MEDICAL_RULES`에서 수정할 수 있습니다.

### 검증 기준의 근거

- 보건복지부·대한신장학회 「인공신장실 설치 및 운영 세부기준 권고안」: 침상 간 0.8m 이상, **병상당 6m² 이상**, 격리실 1개 이상 → 병상당 면적 검사에 반영
- 의료법 시행규칙 [별표4] 입원실: 병상 간 1.5m(신·증축) / 기존 시설 특례 1.0m → 이격 거리 기본값 1.0m 채택
- 장비 카탈로그의 투석기 치수(Fresenius 4008S/6008, Baxter AK98, Nipro Surdial X, 투석 체어)는 제조사 기술자료 실측값(cm)

## Auto Modeling 배치 원리 (실제 도면 분석 기반)

첨부된 실제 인공신장실 도면 3장을 분석해 도출한 규칙이 알고리즘에 반영되어 있습니다.

1. **스테이션 시야**: 간호사실은 주 출입구 옆에 개방 카운터(문 없음, 개방면 표시)로 배치되어 출입구와 병상 필드를 동시에 관찰한다.
2. **소음 격리**: 정수실(RO)은 병상 밀집 구역에서 가장 먼 기술 밴드 최하단 코너에 배치한다.
3. **면적 배분 1:1**: 부속시설 면적이 전체의 약 50%가 되도록 실 크기를 배율(k)로 확대한다.
4. **콘솔 양면 배치**: 하나의 배관 콘솔을 사이에 두고 위(머리↓)/아래(머리↑) 양방향으로 병상을 설치한다(back-to-back). 콘솔로 분리된 병상 쌍은 이격 거리 검증에서 예외 처리된다.
5. **실 특성별 내/외부 배치**: 오염·설비 계열(오물·세탁·기구세척·정수·코어)은 외벽 쪽 기술 밴드에 아래(오염)→위(청결) 순으로, 환자 접근 실은 하단 밴드에 안쪽(조제·처치)→출입구 쪽(화장실→탈의→대기→스테이션) 순으로 배치한다.
6. **오물처리실 양방향 출구**: 내부 복도 문 + 외벽 반출구 두 개의 문을 단다.
7. **문 앞 통로**: 모든 출입문을 통과하면 바로 통로가 되도록 기술 밴드 앞 세로 복도, 환자 밴드 앞 가로 복도(각 1,300mm)와 출입구 세로 주 동선을 병상 금지 구역으로 확보한다.
8. **동선-배관 분리**: 주 동선을 병상 필드 가장자리에 배치해 배관 주행선이 통로를 가로지르지 않고, 트렁크 배관은 기술 밴드 벽체 매입 체이스로, 수평 주행은 콘솔(바닥 트렌치) 내부로만 지난다.
9. **계통 분리**: 정수실 내부 배관과 신장실 배관은 서로 연결하지 않는다 — 신장실 트렁크는 정수실 벽면의 「RO 인계점」에서 시작한다.
10. **실별 기본 오브젝트**: 각 실 용도에 맞는 집기(스테이션 2인 데스크 유닛, 탈의실 락커, 화장실 변기·세면대, 대기실 의자, 창고 선반, 세척 싱크, 격리실 손세정대)를 자동 배치하며, 문 개폐 구역과 겹치면 반대편으로 회피해 **오브젝트가 문 입구를 막지 않는다**.
11. **간호 스테이션 좌석**: 2인 책상+의자 유닛 단위로 2/4/6/8석을 선택해 스테이션 내부에 배치한다. 스테이션은 출입구 옆 + 병상 필드 개방면 위치.
12. **보조 동선**: 주 동선에서 각 병상 통로·환자 밴드 앞 복도로 이어지는 간호(치료) 이동로를 초록 점선 양방향 화살표로 표시한다.
13. **N.S 아일랜드 (2차 도면 분석)**: 간호 스테이션은 밴드 속 '방'이 아니라 병상 필드 안에 독립 배치된 **U자형 개방 카운터**다. 개방면이 병상 쪽을 향해 모든 병상 열을 관찰하고, 주 동선에 접해 출입구·처치실과의 이동이 짧다. 내부에 2인 데스크 유닛이 들어간다.
14. **처치실·조제실의 N.S 인접**: 간호처치실과 조제실은 환자 밴드의 동선 쪽 끝(=N.S와 같은 복도 선상)에 배치해 직원 동선을 최소화하고, 환자 프런트(화장실·탈의실·대기실)는 그 반대편에 둬 동선이 교차하지 않는다.
15. **정수실 내부 자동 모델링**: 원수 인입 → 이송펌프 → Multimedia/Softner/Carbon 전처리 3연 → 5μ 필터 → RO(CWP106H/CWP66) 처리 순서와 Heating·Control Box·Main Panel·배수구를 자동 배치한다.

## 참고한 설계 기준 (웹 조사)

자동 배치 규칙의 근거로 조사한 국내외 기준·사례입니다. 조사 환경의 프록시 제약으로 원문 PDF는 열람하지 못하고 검색 요약 기준으로 수집했으므로, 실제 설계 적용 전에는 원문 확인이 필요합니다.

| 출처 | 핵심 수치 |
|---|---|
| 보건복지부 인공신장실 권고안(2021, 비구속 권고) | 병상당 **6m² 이상**(간호사 스테이션·내부 복도 포함, 간호사실·기기창고·청결실·오물실·린넨실 등 지원실 제외 — 검증 로직에 동일 정의 적용). 침상 간 0.8m·격리실 1실은 2차 조사에서 원문 미확인 → 참고 표기 |
| 질병관리청 인공신장실 감염관리지침(제2판) | 코호트 격리 시 병상 간 **1.0m 이상** + 물리적 칸막이 |
| 의료법 시행규칙 [별표4] | 입원실 병상 간 신·증축 **1.5m** / 기존 **1.0m** (인공신장실 전용 조문 아님) |
| FGI Guidelines (미국) | 병상당 **80 sq ft(7.4m²)**, 병상 주위·사이 **4ft(1220mm)**, 간호 스테이션 80 sq ft·전 병상 관찰 가능, 격리실 **120 sq ft**, 자가투석 교육실 120 sq ft |
| AusHFG (호주) | 투석 베이 **9m²**(체어) / **12m²**(베드 비중 50% 초과), 베이 후면 **3m 유효폭**, **베이 4개당 손세정대 1개**, 전 베이에서 스테이션 시야 확보 |
| CDC | 정규 배치 이격 기준 없음. 호흡기 주의 시 **6ft(1830mm)**, HBV 양성은 **별실 + 전용 장비** |
| 일본 투석 전문 설계 사례 | 베드 간 **80cm**(관행) → 최근 계획 **약 2m**, 통로 **1.2m**(베드 옆까지 휠체어 진입), 개실 미닫이 개구 **1.75m**(베드 반출입) |
| 실제 프로젝트 | 병상당 연면적 **약 400~550 sq ft**(미국 20~30병상 클리닉), 등맞댐 포드 배치(Stantec 68병상), 포드당 8~10석·간호사 1:8 |

반영 결과: 손세정대 **병상 4대당 1개** 자동 배치, 투석기 정비실·자가투석 교육실 에셋 추가, 격리실을 모듈 길이 + 발쪽 800mm이 확보되도록 자동 확대, 대기실을 출입구와 직결, 병상당 면적 검증을 권고안 정의(지원실 면적 제외)로 정밀화.

추가 확인(2차 조사, VA PG-18-9 원문 검증): 간호 스테이션 1개당 관찰 병상 **최대 10대**(11대 이상은 스테이션 2개), 헤드월 모듈 **3.28m**, 체어 스테이션 80NSF/베드 100NSF, 격리 스테이션은 **높이 1.5m 이상 격벽 + 유리 미닫이 + 손세정대**, 청결↔오염 **일방향 흐름**. 이 중 스테이션 다중화·시야 기하 검증은 다음 단계 반영 예정입니다.

## 업로드 도면 분석 (reference-plans/)

`reference-plans/25BED.png`(대동병원 25EA)·`27bed.png` 실측 분석 결과와 반영 현황:

| 실측 항목 | 도면 수치 | 반영 |
|---|---|---|
| 병상 피치 | 1,500~1,520(25EA) / 1,800(27bed) | 모듈 폭 기본 1,800, 1,750~3,000 설정 가능 ✓ |
| 등맞댐 콘솔 폭 | **640** | 콘솔 기본값 250→**600mm** 조정 ✓ |
| 병상 통로 | 1,160 / 1,280 / 1,360 / 1,430 / 1,700 | 통로 폭 기본 1,500 설정 가능 ✓ |
| 중앙 로비(주 동선) | 3,370 | 주 동선 = 통로 폭 연동(확대 여지 — 후속) |
| N.S | 긴 카운터 + 의자 1열(5석/3석), 병상 필드 지향 | 1열 데스크 N.S ✓ |
| N.S 인접 실 | 간호처치실·조제실 바로 옆 | 동선 쪽 끝 배치 ✓ |
| RO실 | 필드 끝 코너, 창고와 인접 | 후방 밴드 최하단 코너 ✓ |
| 격리실 | 필드 가장자리 2실(일반+중환자) | 1실 자동(개수 확장 — 후속) |

## 확장 아이디어

- `assets.js`에 장비 항목만 추가하면 툴바 버튼·범례가 자동 생성됩니다.
- 장비 아이콘 이미지 적용: `addEquipment()`에서 `fabric.Rect` 대신 `fabric.Image` 사용.
- 축척 표기 PDF(1:50 등), DXF 내보내기, 다중 층 지원 등.
